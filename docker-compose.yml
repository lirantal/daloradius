# Root docker-compose.yml placeholder.
# Run setup/install-docker-compose.sh to generate the full orchestrator.

services:
  setup-required:
    image: alpine:3.21
    container_name: setup-required
    restart: "no"
    entrypoint: >
      sh -c 'echo "ERROR: Run setup/install-docker-compose.sh first to generate the full orchestrator." && exit 1'
